image: ruby:3.3-bookworm
command:
  - "/bin/bash"
  - "-c"
  - "export RUBYLIB=./lib && (bundle config set path /script/bundle > /dev/null) && bundle exec ruby script.rb"
# egress: true
# workDir: "/script"